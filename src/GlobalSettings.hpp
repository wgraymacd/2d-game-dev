#pragma once

#include "Vec2.hpp"

namespace GlobalSettings
{
    inline Vec2ui windowSize = { 1280, 720 };
    inline int frameRate = 120;
    inline Vec2ui worldMaxCells = { 1000, 400 };
    inline Vec2ui cellSizePixels = { 10, 10 };
}